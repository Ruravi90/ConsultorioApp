<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ConsultorioApp.ViewModels"
             x:Class="ConsultorioApp.Views.RolesPage"
             Title="Roles de Usuarios">

    <ScrollView>
        <VerticalStackLayout Padding="20">
            <!-- Lista de roles -->
            <CollectionView ItemsSource="{Binding Roles}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame CornerRadius="8" Margin="0,10,0,0">
                            <VerticalStackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold"/>
                                <Button Text="Eliminar" Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:RolesViewModel}}, Path=EliminarRolCommand}" CommandParameter="{Binding .}" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Botón: Nuevo Rol -->
            <Button Text="Nuevo Rol"
                    Command="{Binding NuevoRolCommand}"
                    BackgroundColor="#007AFF"
                    TextColor="White"
                    CornerRadius="8"
                    HeightRequest="50"
                    Margin="0,20,0,0" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>