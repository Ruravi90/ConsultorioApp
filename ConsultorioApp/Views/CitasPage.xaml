<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ConsultorioApp.ViewModels"
             x:DataType="viewmodels:CitasViewModel"
             x:Class="ConsultorioApp.Views.CitasPage"
             Title="Lista de Citas">
    
    <ScrollView>
        <VerticalStackLayout Padding="20">
            <Button Text="Nueva Cita" Command="{Binding NuevaCitaCommand}" />

            <CollectionView ItemsSource="{Binding Citas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="10" Spacing="5">
                            <Label Text="{Binding Paciente.Nombre}" FontAttributes="Bold" />
                            <Label Text="{Binding Motivo}" />
                            <Label Text="{Binding Fecha, StringFormat='{0:dd/MM/yyyy HH:mm}'}" />
                            <Button Text="Ver Detalle"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CitasViewModel}}, Path=IrADetalleCommand}"
                                    CommandParameter="{Binding .}" />
                            <Button Text="Eliminar"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CitasViewModel}}, Path=EliminarCommand}"
                                    CommandParameter="{Binding .}" />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>